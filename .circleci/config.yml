version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.23.3-stretch
    steps:
      - checkout
      - run:
          name: Install packages
          command: yarn install
      - run:
          name: Build, Minify, & Run app
          command: yarn deploy
