version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.23.2-stretch
    steps:
      - checkout
      - run:
          name: Install packages
          command: yarn install
      - run:
          name: Build, Minify, & Run app
          command: yarn deploy
